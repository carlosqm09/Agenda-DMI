import 'package:flutter/material.dart';
import 'package:agenda_dmi/api_terceros/api.dart';

class ApiTerc extends StatelessWidget {
  @override
  Widget build(context) {
    return MaterialApp(
      home: Center(
        child: HomePage(),
      ),
    );
  }
}
